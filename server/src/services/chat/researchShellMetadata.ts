/**
 * Meeting spec §2.4 / §4.6 — signal for COHI-404 to auto-expand shell to full page
 * on research-driving submit (initial investigation query).
 */
export const RESEARCH_SHELL_EXPAND_METADATA = {
  researchShellExpand: true,
} as const;

# Increase the maximum allowed size for client request body
# This allows uploads of larger CSV/Excel files for loan imports
client_max_body_size 100M;

# Increase proxy timeouts to handle large file uploads
proxy_connect_timeout 300;
proxy_send_timeout 300;
proxy_read_timeout 300;
send_timeout 300;

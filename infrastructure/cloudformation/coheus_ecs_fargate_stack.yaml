AWSTemplateFormatVersion: "2010-09-09"
Description: |
  Coheus ECS Fargate Backend Stack (Compute Only)

  This stack deploys:
  - VPC with public/private subnets (or uses existing)
  - ECS Fargate cluster and service
  - Application Load Balancer
  - CloudWatch monitoring and alarms

  Database: Deploy separately using coheus_aurora_cluster_stack.yaml

Parameters:
  ProjectName:
    Type: String
    Default: coheus
    Description: Project name for resource naming

  Environment:
    Type: String
    Default: prod
    AllowedValues:
      - dev
      - staging
      - prod
    Description: Environment name

  # Network Configuration
  NetworkMode:
    Type: String
    Default: new
    AllowedValues:
      - new
      - existing
    Description: Create new VPC or use existing

  ExistingVPCId:
    Type: String
    Default: ""
    Description: Existing VPC ID (required if NetworkMode=existing)

  ExistingPrivateSubnet1:
    Type: String
    Default: ""
    Description: Existing private subnet 1 (required if NetworkMode=existing)

  ExistingPrivateSubnet2:
    Type: String
    Default: ""
    Description: Existing private subnet 2 (required if NetworkMode=existing)

  ExistingPublicSubnet1:
    Type: String
    Default: ""
    Description: Existing public subnet 1 (required if NetworkMode=existing)

  ExistingPublicSubnet2:
    Type: String
    Default: ""
    Description: Existing public subnet 2 (required if NetworkMode=existing)

  # Container Configuration
  ContainerImageTag:
    Type: String
    Default: latest
    Description: Docker image tag to deploy

  ContainerPort:
    Type: Number
    Default: 3001
    Description: Port the container listens on

  ContainerCpu:
    Type: Number
    Default: 2048
    AllowedValues:
      - 256
      - 512
      - 1024
      - 2048
      - 4096
    Description: CPU units (256 = 0.25 vCPU, 1024 = 1 vCPU)

  ContainerMemory:
    Type: Number
    Default: 4096
    AllowedValues:
      - 512
      - 1024
      - 2048
      - 4096
      - 8192
    Description: Memory in MB

  DesiredCount:
    Type: Number
    Default: 2
    MinValue: 0
    MaxValue: 20
    Description: Desired number of tasks (0 for initial deployment before image push)

  MinCount:
    Type: Number
    Default: 2
    MinValue: 0
    Description: Minimum tasks for auto-scaling (0 for initial deployment)

  MaxCount:
    Type: Number
    Default: 10
    MinValue: 1
    Description: Maximum tasks for auto-scaling

  # Sync worker (dedicated container for Encompass ETL)
  WorkerCpu:
    Type: Number
    Default: 2048
    AllowedValues:
      - 512
      - 1024
      - 2048
      - 4096
    Description: CPU units for worker tasks (ETL workloads need more CPU than API)

  WorkerMemory:
    Type: Number
    Default: 4096
    AllowedValues:
      - 1024
      - 2048
      - 4096
      - 8192
    Description: Memory in MB for worker tasks

  WorkerDesiredCount:
    Type: Number
    Default: 1
    MinValue: 0
    MaxValue: 4
    Description: Desired number of worker tasks (0 to disable dedicated worker)

  # Aurora Database Configuration (required)
  AuroraEndpoint:
    Type: String
    Description: Aurora cluster endpoint (from Aurora stack output)

  AuroraSecretArn:
    Type: String
    Description: Secrets Manager ARN for Aurora credentials (from Aurora stack output)

  AuroraKmsKeyArn:
    Type: String
    Description: KMS key ARN used to encrypt the Aurora secret (from Aurora stack KMSKeyArn output). Required so ECS can decrypt the secret.

  # Application Configuration
  JwtSecret:
    Type: String
    NoEcho: true
    MinLength: 32
    Description: JWT signing secret (min 32 characters)

  FrontendUrl:
    Type: String
    Default: ""
    Description: Frontend URL for CORS

  # SSL Configuration
  CertificateArn:
    Type: String
    Default: ""
    Description: ACM certificate ARN for HTTPS (optional)

  # Alerts
  AlertEmail:
    Type: String
    Default: ""
    Description: Email for alerts (optional)

  # Cognito SSO Configuration
  CognitoUserPoolId:
    Type: String
    Default: ""
    Description: Cognito User Pool ID for SSO authentication

  CognitoClientId:
    Type: String
    Default: ""
    Description: Cognito App Client ID

  CognitoClientSecret:
    Type: String
    NoEcho: true
    Default: ""
    Description: Cognito App Client Secret

  CognitoDomain:
    Type: String
    Default: ""
    Description: Cognito domain (e.g., your-app.auth.us-east-2.amazoncognito.com)

  CognitoPasswordAuth:
    Type: String
    Default: "false"
    AllowedValues: ["true", "false"]
    Description: Enable Cognito for email/password auth (requires ALLOW_ADMIN_USER_PASSWORD_AUTH on app client)

  # Email / SES Configuration
  EmailFromAddress:
    Type: String
    Default: "noreply@coheus1.com"
    Description: From address for outbound emails (must be a verified SES identity)

  # Platform SSO
  PlatformSsoIdpName:
    Type: String
    Default: ""
    Description: Cognito IdP name for platform staff SSO (e.g., TestAccCognito)

  PlatformJitDomains:
    Type: String
    Default: "teraverde.com"
    Description: Comma-separated email domains allowed for platform JIT provisioning

  # FRED API (market rates)
  FredApiKey:
    Type: String
    NoEcho: true
    Default: ""
    Description: FRED API key for fetching mortgage market rates (optional)

  OpenAIApiKeySecretArn:
    Type: String
    Default: ""
    Description: Optional Secrets Manager ARN whose SecretString is the raw OpenAI API key

Conditions:
  CreateNewVPC: !Equals [!Ref NetworkMode, "new"]
  IsDevEnvironment: !Equals [!Ref Environment, "dev"]
  HasCertificate: !Not [!Equals [!Ref CertificateArn, ""]]
  HasAlertEmail: !Not [!Equals [!Ref AlertEmail, ""]]
  HasCognito: !Not [!Equals [!Ref CognitoUserPoolId, ""]]
  HasOpenAISecret: !Not [!Equals [!Ref OpenAIApiKeySecretArn, ""]]
  WorkerDesiredCountNonZero: !Not [!Equals [!Ref WorkerDesiredCount, 0]]

Rules:
  ExistingVPCRequiresSubnets:
    RuleCondition: !Equals [!Ref NetworkMode, "existing"]
    Assertions:
      - Assert: !Not [!Equals [!Ref ExistingVPCId, ""]]
        AssertDescription: ExistingVPCId is required when NetworkMode=existing
      - Assert: !Not [!Equals [!Ref ExistingPrivateSubnet1, ""]]
        AssertDescription: ExistingPrivateSubnet1 is required when NetworkMode=existing
      - Assert: !Not [!Equals [!Ref ExistingPrivateSubnet2, ""]]
        AssertDescription: ExistingPrivateSubnet2 is required when NetworkMode=existing
      - Assert: !Not [!Equals [!Ref ExistingPublicSubnet1, ""]]
        AssertDescription: ExistingPublicSubnet1 is required when NetworkMode=existing
      - Assert: !Not [!Equals [!Ref ExistingPublicSubnet2, ""]]
        AssertDescription: ExistingPublicSubnet2 is required when NetworkMode=existing

Resources:
  # ============================================================================
  # KMS Key for Encryption
  # ============================================================================
  EncryptionKey:
    Type: AWS::KMS::Key
    Properties:
      Description: !Sub "KMS key for ${ProjectName} ${Environment} encryption"
      EnableKeyRotation: true
      KeyPolicy:
        Version: "2012-10-17"
        Statement:
          - Sid: Enable IAM User Permissions
            Effect: Allow
            Principal:
              AWS: !Sub "arn:aws:iam::${AWS::AccountId}:root"
            Action: "kms:*"
            Resource: "*"
          - Sid: Allow CloudWatch Logs
            Effect: Allow
            Principal:
              Service: !Sub "logs.${AWS::Region}.amazonaws.com"
            Action:
              - "kms:Encrypt"
              - "kms:Decrypt"
              - "kms:GenerateDataKey*"
            Resource: "*"
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-${Environment}-key"

  # ============================================================================
  # ECR Repository
  # ============================================================================
  ECRRepository:
    Type: AWS::ECR::Repository
    UpdateReplacePolicy: Retain
    DeletionPolicy: Retain
    Properties:
      RepositoryName: !If
        - IsDevEnvironment
        - !Sub "${ProjectName}-backend"
        - !Sub "${ProjectName}-${Environment}-backend"
      ImageScanningConfiguration:
        ScanOnPush: true
      EncryptionConfiguration:
        EncryptionType: AES256
      ImageTagMutability: MUTABLE
      LifecyclePolicy:
        LifecyclePolicyText: |
          {
            "rules": [
              {
                "rulePriority": 1,
                "description": "Keep last 10 images",
                "selection": {
                  "tagStatus": "any",
                  "countType": "imageCountMoreThan",
                  "countNumber": 10
                },
                "action": {
                  "type": "expire"
                }
              }
            ]
          }
      Tags:
        - Key: Name
          Value: !If
            - IsDevEnvironment
            - !Sub "${ProjectName}-backend"
            - !Sub "${ProjectName}-${Environment}-backend"

  # ============================================================================
  # VPC and Networking (Conditional)
  # ============================================================================
  VPC:
    Type: AWS::EC2::VPC
    Condition: CreateNewVPC
    Properties:
      CidrBlock: 10.0.0.0/16
      EnableDnsHostnames: true
      EnableDnsSupport: true
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-${Environment}-vpc"

  PublicSubnet1:
    Type: AWS::EC2::Subnet
    Condition: CreateNewVPC
    Properties:
      VpcId: !Ref VPC
      AvailabilityZone: !Select [0, !GetAZs ""]
      CidrBlock: 10.0.10.0/24
      MapPublicIpOnLaunch: true
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-${Environment}-public-1"

  PublicSubnet2:
    Type: AWS::EC2::Subnet
    Condition: CreateNewVPC
    Properties:
      VpcId: !Ref VPC
      AvailabilityZone: !Select [1, !GetAZs ""]
      CidrBlock: 10.0.11.0/24
      MapPublicIpOnLaunch: true
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-${Environment}-public-2"

  PrivateSubnet1:
    Type: AWS::EC2::Subnet
    Condition: CreateNewVPC
    Properties:
      VpcId: !Ref VPC
      AvailabilityZone: !Select [0, !GetAZs ""]
      CidrBlock: 10.0.1.0/24
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-${Environment}-private-1"

  PrivateSubnet2:
    Type: AWS::EC2::Subnet
    Condition: CreateNewVPC
    Properties:
      VpcId: !Ref VPC
      AvailabilityZone: !Select [1, !GetAZs ""]
      CidrBlock: 10.0.2.0/24
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-${Environment}-private-2"

  InternetGateway:
    Type: AWS::EC2::InternetGateway
    Condition: CreateNewVPC
    Properties:
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-${Environment}-igw"

  InternetGatewayAttachment:
    Type: AWS::EC2::VPCGatewayAttachment
    Condition: CreateNewVPC
    Properties:
      VpcId: !Ref VPC
      InternetGatewayId: !Ref InternetGateway

  NatGatewayEIP:
    Type: AWS::EC2::EIP
    Condition: CreateNewVPC
    DependsOn: InternetGatewayAttachment
    Properties:
      Domain: vpc
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-${Environment}-nat-eip"

  NatGateway:
    Type: AWS::EC2::NatGateway
    Condition: CreateNewVPC
    Properties:
      AllocationId: !GetAtt NatGatewayEIP.AllocationId
      SubnetId: !Ref PublicSubnet1
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-${Environment}-nat"

  PublicRouteTable:
    Type: AWS::EC2::RouteTable
    Condition: CreateNewVPC
    Properties:
      VpcId: !Ref VPC
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-${Environment}-public-rt"

  DefaultPublicRoute:
    Type: AWS::EC2::Route
    Condition: CreateNewVPC
    DependsOn: InternetGatewayAttachment
    Properties:
      RouteTableId: !Ref PublicRouteTable
      DestinationCidrBlock: 0.0.0.0/0
      GatewayId: !Ref InternetGateway

  PublicSubnet1RouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Condition: CreateNewVPC
    Properties:
      SubnetId: !Ref PublicSubnet1
      RouteTableId: !Ref PublicRouteTable

  PublicSubnet2RouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Condition: CreateNewVPC
    Properties:
      SubnetId: !Ref PublicSubnet2
      RouteTableId: !Ref PublicRouteTable

  PrivateRouteTable:
    Type: AWS::EC2::RouteTable
    Condition: CreateNewVPC
    Properties:
      VpcId: !Ref VPC
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-${Environment}-private-rt"

  DefaultPrivateRoute:
    Type: AWS::EC2::Route
    Condition: CreateNewVPC
    Properties:
      RouteTableId: !Ref PrivateRouteTable
      DestinationCidrBlock: 0.0.0.0/0
      NatGatewayId: !Ref NatGateway

  PrivateSubnet1RouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Condition: CreateNewVPC
    Properties:
      SubnetId: !Ref PrivateSubnet1
      RouteTableId: !Ref PrivateRouteTable

  PrivateSubnet2RouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Condition: CreateNewVPC
    Properties:
      SubnetId: !Ref PrivateSubnet2
      RouteTableId: !Ref PrivateRouteTable

  # ============================================================================
  # Security Groups
  # ============================================================================
  ALBSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Security group for Application Load Balancer
      VpcId: !If [CreateNewVPC, !Ref VPC, !Ref ExistingVPCId]
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 443
          ToPort: 443
          CidrIp: 0.0.0.0/0
          Description: Allow HTTPS
        - IpProtocol: tcp
          FromPort: 80
          ToPort: 80
          CidrIp: 0.0.0.0/0
          Description: Allow HTTP
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-${Environment}-alb-sg"

  ECSSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Security group for ECS tasks
      VpcId: !If [CreateNewVPC, !Ref VPC, !Ref ExistingVPCId]
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: !Ref ContainerPort
          ToPort: !Ref ContainerPort
          SourceSecurityGroupId: !Ref ALBSecurityGroup
          Description: Allow traffic from ALB
      SecurityGroupEgress:
        - IpProtocol: -1
          CidrIp: 0.0.0.0/0
          Description: Allow all outbound
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-${Environment}-ecs-sg"

  # ============================================================================
  # Secrets Manager - JWT Secret
  # ============================================================================
  JWTSecret:
    Type: AWS::SecretsManager::Secret
    Properties:
      Name: !Sub "${ProjectName}/${Environment}/jwt"
      Description: JWT signing secret
      KmsKeyId: !Ref EncryptionKey
      SecretString: !Sub '{"secret":"${JwtSecret}"}'

  PodcastAudioBucket:
    Type: AWS::S3::Bucket
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      BucketName: !Sub "${ProjectName}-${Environment}-podcast-audio-${AWS::AccountId}"
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: aws:kms
              KMSMasterKeyID: !GetAtt EncryptionKey.Arn
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      VersioningConfiguration:
        Status: Enabled
      LifecycleConfiguration:
        Rules:
          - Id: ExpireOldPodcastAssets
            Status: Enabled
            ExpirationInDays: 14
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-${Environment}-podcast-audio"

  # ============================================================================
  # Application Load Balancer
  # ============================================================================
  ApplicationLoadBalancer:
    Type: AWS::ElasticLoadBalancingV2::LoadBalancer
    Properties:
      Name: !Sub "${ProjectName}-${Environment}-alb"
      Type: application
      Scheme: internet-facing
      SecurityGroups:
        - !Ref ALBSecurityGroup
      Subnets:
        - !If [CreateNewVPC, !Ref PublicSubnet1, !Ref ExistingPublicSubnet1]
        - !If [CreateNewVPC, !Ref PublicSubnet2, !Ref ExistingPublicSubnet2]
      LoadBalancerAttributes:
        - Key: idle_timeout.timeout_seconds
          Value: "3600"
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-${Environment}-alb"

  ALBTargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Name: !Sub "${ProjectName}-${Environment}-tg"
      Port: !Ref ContainerPort
      Protocol: HTTP
      VpcId: !If [CreateNewVPC, !Ref VPC, !Ref ExistingVPCId]
      TargetType: ip
      HealthCheckPath: /health
      HealthCheckIntervalSeconds: 30
      HealthyThresholdCount: 2
      UnhealthyThresholdCount: 5
      TargetGroupAttributes:
        - Key: deregistration_delay.timeout_seconds
          Value: "30"
        - Key: stickiness.enabled
          Value: "true"
        - Key: stickiness.type
          Value: lb_cookie
        - Key: stickiness.lb_cookie.duration_seconds
          Value: "86400"
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-${Environment}-tg"

  ALBHTTPSListener:
    Type: AWS::ElasticLoadBalancingV2::Listener
    Condition: HasCertificate
    Properties:
      LoadBalancerArn: !Ref ApplicationLoadBalancer
      Port: 443
      Protocol: HTTPS
      SslPolicy: ELBSecurityPolicy-TLS13-1-2-2021-06
      Certificates:
        - CertificateArn: !Ref CertificateArn
      DefaultActions:
        - Type: forward
          TargetGroupArn: !Ref ALBTargetGroup

  ALBHTTPListener:
    Type: AWS::ElasticLoadBalancingV2::Listener
    Properties:
      LoadBalancerArn: !Ref ApplicationLoadBalancer
      Port: 80
      Protocol: HTTP
      DefaultActions:
        - !If
          - HasCertificate
          - Type: redirect
            RedirectConfig:
              Protocol: HTTPS
              Port: "443"
              StatusCode: HTTP_301
          - Type: forward
            TargetGroupArn: !Ref ALBTargetGroup

  # ============================================================================
  # ECS Cluster and Service
  # ============================================================================
  ECSCluster:
    Type: AWS::ECS::Cluster
    Properties:
      ClusterName: !Sub "${ProjectName}-${Environment}-cluster"
      ClusterSettings:
        - Name: containerInsights
          Value: enabled
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-${Environment}-cluster"

  ECSLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub "/ecs/${ProjectName}-${Environment}"
      RetentionInDays: 30
      KmsKeyId: !GetAtt EncryptionKey.Arn

  ECSTaskExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub "${ProjectName}-${Environment}-ecs-exec-role"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: ecs-tasks.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AmazonECSTaskExecutionRolePolicy
      Policies:
        - PolicyName: SecretsAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - secretsmanager:GetSecretValue
                Resource:
                  - !Ref AuroraSecretArn
                  - !Ref JWTSecret
                  - !If [HasOpenAISecret, !Ref OpenAIApiKeySecretArn, !Ref AWS::NoValue]
              - Effect: Allow
                Action:
                  - kms:Decrypt
                Resource: !GetAtt EncryptionKey.Arn
              # Aurora secret is encrypted with Aurora stack's KMS key; ECS must decrypt to fetch DB credentials
              - Effect: Allow
                Action:
                  - kms:Decrypt
                Resource: !Ref AuroraKmsKeyArn

  ECSTaskRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub "${ProjectName}-${Environment}-ecs-task-role"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: ecs-tasks.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: TaskPolicy
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - secretsmanager:GetSecretValue
                Resource:
                  - !Ref AuroraSecretArn
                  - !Ref JWTSecret
                  - !If [HasOpenAISecret, !Ref OpenAIApiKeySecretArn, !Ref AWS::NoValue]
              - Effect: Allow
                Action:
                  - kms:Decrypt
                  - kms:Encrypt
                  - kms:GenerateDataKey
                Resource: !GetAtt EncryptionKey.Arn
              - Effect: Allow
                Action:
                  - s3:ListBucket
                Resource: !GetAtt PodcastAudioBucket.Arn
              - Effect: Allow
                Action:
                  - s3:GetObject
                  - s3:PutObject
                  - s3:DeleteObject
                Resource: !Sub "${PodcastAudioBucket.Arn}/*"
              # ECS Exec permissions (for running migrations via aws ecs execute-command)
              - Effect: Allow
                Action:
                  - ssmmessages:CreateControlChannel
                  - ssmmessages:CreateDataChannel
                  - ssmmessages:OpenControlChannel
                  - ssmmessages:OpenDataChannel
                Resource: "*"
              - Effect: Allow
                Action:
                  - logs:CreateLogStream
                  - logs:DescribeLogGroups
                  - logs:DescribeLogStreams
                  - logs:PutLogEvents
                  - logs:FilterLogEvents
                  - logs:GetLogEvents
                Resource: "*"
              - Effect: Allow
                Action:
                  - ses:SendEmail
                  - ses:SendRawEmail
                Resource:
                  - !Sub "arn:aws:ses:${AWS::Region}:${AWS::AccountId}:identity/*"
                  - !Sub "arn:aws:ses:${AWS::Region}:${AWS::AccountId}:configuration-set/*"
              - !If
                - HasCognito
                - Effect: Allow
                  Action:
                    - cognito-idp:AdminInitiateAuth
                    - cognito-idp:AdminRespondToAuthChallenge
                    - cognito-idp:AdminCreateUser
                    - cognito-idp:AdminDeleteUser
                    - cognito-idp:AdminSetUserPassword
                    - cognito-idp:AdminSetUserMFAPreference
                    - cognito-idp:AdminGetUser
                    - cognito-idp:AdminResetUserPassword
                    - cognito-idp:AssociateSoftwareToken
                    - cognito-idp:VerifySoftwareToken
                    - cognito-idp:ForgotPassword
                    - cognito-idp:ConfirmForgotPassword
                    - cognito-idp:DescribeUserPool
                    - cognito-idp:GetUserPoolMfaConfig
                  Resource: !Sub "arn:aws:cognito-idp:${AWS::Region}:${AWS::AccountId}:userpool/${CognitoUserPoolId}"
                - !Ref "AWS::NoValue"

  ECSTaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub "${ProjectName}-${Environment}-backend"
      NetworkMode: awsvpc
      RequiresCompatibilities:
        - FARGATE
      Cpu: !Ref ContainerCpu
      Memory: !Ref ContainerMemory
      ExecutionRoleArn: !GetAtt ECSTaskExecutionRole.Arn
      TaskRoleArn: !GetAtt ECSTaskRole.Arn
      ContainerDefinitions:
        - Name: !Sub "${ProjectName}-backend"
          Image: !Sub "${AWS::AccountId}.dkr.ecr.${AWS::Region}.amazonaws.com/${ECRRepository}:${ContainerImageTag}"
          Essential: true
          PortMappings:
            - ContainerPort: !Ref ContainerPort
              Protocol: tcp
          Command:
            - "npm"
            - "start"
          Environment:
            - Name: NODE_OPTIONS
              Value: "--max-old-space-size=3584"
            - Name: NODE_ENV
              Value: production
            - Name: PROCESS_MODE
              Value: api
            - Name: PORT
              Value: !Ref ContainerPort
            - Name: DEPLOYMENT_MODE
              Value: saas
            - Name: MULTI_TENANT_ENABLED
              Value: "true"
            - Name: DB_HOST
              Value: !Ref AuroraEndpoint
            - Name: DB_PORT
              Value: "5432"
            - Name: DB_NAME
              Value: coheus_management
            - Name: DB_SSL
              Value: "true"
            - Name: FRONTEND_URL
              Value: !Ref FrontendUrl
            - Name: KMS_KEY_ID
              Value: !Ref EncryptionKey
            - Name: AWS_REGION
              Value: !Ref AWS::Region
            - Name: JWT_SECRET
              Value: !Ref JwtSecret
            # Cognito SSO Configuration (optional)
            - Name: COGNITO_USER_POOL_ID
              Value: !If [HasCognito, !Ref CognitoUserPoolId, ""]
            - Name: COGNITO_CLIENT_ID
              Value: !If [HasCognito, !Ref CognitoClientId, ""]
            - Name: COGNITO_CLIENT_SECRET
              Value: !If [HasCognito, !Ref CognitoClientSecret, ""]
            - Name: COGNITO_REGION
              Value: !If [HasCognito, !Ref AWS::Region, ""]
            - Name: COGNITO_DOMAIN
              Value: !If [HasCognito, !Ref CognitoDomain, ""]
            - Name: COGNITO_PASSWORD_AUTH
              Value: !If [HasCognito, !Ref CognitoPasswordAuth, "false"]
            # Email / SES (use existing SES configuration set for reputation metrics; add SNS event destination for bounce/complaint handling if needed)
            - Name: AWS_SES_REGION
              Value: !Ref AWS::Region
            - Name: EMAIL_FROM_ADDRESS
              Value: !Ref EmailFromAddress
            - Name: SES_CONFIGURATION_SET
              Value: my-first-configuration-set
            # Platform SSO
            - Name: PLATFORM_SSO_IDP_NAME
              Value: !Ref PlatformSsoIdpName
            - Name: PLATFORM_JIT_DOMAINS
              Value: !Ref PlatformJitDomains
            # FRED API for market rate data
            - Name: FRED_API_KEY
              Value: !Ref FredApiKey
            - Name: PODCAST_AUDIO_BUCKET
              Value: !Ref PodcastAudioBucket
            - Name: PODCAST_AUDIO_PREFIX
              Value: !Sub "${Environment}/audio"
            - Name: ALETHEIA_PREFETCH_ASYNC
              Value: "true"
            - Name: ALETHEIA_PREFETCH_WORKER_ENABLED
              Value: "false"
          Secrets:
            - Name: DB_USER
              ValueFrom: !Sub "${AuroraSecretArn}:username::"
            - Name: DB_PASSWORD
              ValueFrom: !Sub "${AuroraSecretArn}:password::"
            - !If
              - HasOpenAISecret
              - Name: OPENAI_API_KEY
                ValueFrom: !Ref OpenAIApiKeySecretArn
              - !Ref AWS::NoValue
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref ECSLogGroup
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: !Ref ProjectName
          HealthCheck:
            Command:
              - CMD-SHELL
              - !Sub "wget --no-verbose --tries=1 --spider http://localhost:${ContainerPort}/health || exit 1"
            Interval: 30
            Timeout: 10
            Retries: 5
            StartPeriod: 120
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-${Environment}-task"

  # ============================================================================
  # Sync worker task (same image, PROCESS_MODE=worker; runs schedulers + sync job poller)
  # ============================================================================
  ECSTaskDefinitionWorker:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub "${ProjectName}-${Environment}-worker"
      NetworkMode: awsvpc
      RequiresCompatibilities:
        - FARGATE
      Cpu: !Ref WorkerCpu
      Memory: !Ref WorkerMemory
      ExecutionRoleArn: !GetAtt ECSTaskExecutionRole.Arn
      TaskRoleArn: !GetAtt ECSTaskRole.Arn
      ContainerDefinitions:
        - Name: !Sub "${ProjectName}-worker"
          Image: !Sub "${AWS::AccountId}.dkr.ecr.${AWS::Region}.amazonaws.com/${ECRRepository}:${ContainerImageTag}"
          Essential: true
          Command:
            - "node"
            - "--max-old-space-size=3584"
            - "dist/index.js"
          Environment:
            - Name: NODE_ENV
              Value: production
            - Name: PROCESS_MODE
              Value: worker
            - Name: DEPLOYMENT_MODE
              Value: saas
            - Name: MULTI_TENANT_ENABLED
              Value: "true"
            - Name: DB_HOST
              Value: !Ref AuroraEndpoint
            - Name: DB_PORT
              Value: "5432"
            - Name: DB_NAME
              Value: coheus_management
            - Name: DB_SSL
              Value: "true"
            - Name: FRONTEND_URL
              Value: !Ref FrontendUrl
            - Name: KMS_KEY_ID
              Value: !Ref EncryptionKey
            - Name: AWS_REGION
              Value: !Ref AWS::Region
            # Email / SES - keep worker env in parity with API container since
            # worker runs the distribution scheduler and sends scheduled emails.
            - Name: AWS_SES_REGION
              Value: !Ref AWS::Region
            - Name: EMAIL_FROM_ADDRESS
              Value: !Ref EmailFromAddress
            - Name: SES_CONFIGURATION_SET
              Value: my-first-configuration-set
            - Name: JWT_SECRET
              Value: !Ref JwtSecret
            - Name: COGNITO_USER_POOL_ID
              Value: !If [HasCognito, !Ref CognitoUserPoolId, ""]
            - Name: COGNITO_CLIENT_ID
              Value: !If [HasCognito, !Ref CognitoClientId, ""]
            - Name: COGNITO_CLIENT_SECRET
              Value: !If [HasCognito, !Ref CognitoClientSecret, ""]
            - Name: COGNITO_REGION
              Value: !If [HasCognito, !Ref AWS::Region, ""]
            - Name: COGNITO_DOMAIN
              Value: !If [HasCognito, !Ref CognitoDomain, ""]
            - Name: FRED_API_KEY
              Value: !Ref FredApiKey
            - Name: PODCAST_AUDIO_BUCKET
              Value: !Ref PodcastAudioBucket
            - Name: PODCAST_AUDIO_PREFIX
              Value: !Sub "${Environment}/audio"
            - Name: ALETHEIA_PREFETCH_ASYNC
              Value: "true"
            - Name: ALETHEIA_PREFETCH_WORKER_ENABLED
              Value: "true"
            - Name: ALETHEIA_PREFETCH_POLL_MS
              Value: "15000"
            - Name: ALETHEIA_PREFETCH_MAX_JOBS_PER_TICK
              Value: "2"
          Secrets:
            - Name: DB_USER
              ValueFrom: !Sub "${AuroraSecretArn}:username::"
            - Name: DB_PASSWORD
              ValueFrom: !Sub "${AuroraSecretArn}:password::"
            - !If
              - HasOpenAISecret
              - Name: OPENAI_API_KEY
                ValueFrom: !Ref OpenAIApiKeySecretArn
              - !Ref AWS::NoValue
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref ECSLogGroup
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: !Sub "${ProjectName}-worker"
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-${Environment}-worker-task"

  ECSServiceWorker:
    Type: AWS::ECS::Service
    Condition: WorkerDesiredCountNonZero
    Properties:
      ServiceName: !Sub "${ProjectName}-${Environment}-worker-service"
      Cluster: !Ref ECSCluster
      TaskDefinition: !Ref ECSTaskDefinitionWorker
      DesiredCount: !Ref WorkerDesiredCount
      LaunchType: FARGATE
      EnableExecuteCommand: true
      NetworkConfiguration:
        AwsvpcConfiguration:
          Subnets:
            - !If [CreateNewVPC, !Ref PrivateSubnet1, !Ref ExistingPrivateSubnet1]
            - !If [CreateNewVPC, !Ref PrivateSubnet2, !Ref ExistingPrivateSubnet2]
          SecurityGroups:
            - !Ref ECSSecurityGroup
          AssignPublicIp: DISABLED
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-${Environment}-worker-service"

  ECSService:
    Type: AWS::ECS::Service
    DependsOn:
      - ALBHTTPListener
    Properties:
      ServiceName: !Sub "${ProjectName}-${Environment}-service"
      Cluster: !Ref ECSCluster
      TaskDefinition: !Ref ECSTaskDefinition
      DesiredCount: !Ref DesiredCount
      LaunchType: FARGATE
      EnableExecuteCommand: true # Enables ECS Exec for running migrations
      NetworkConfiguration:
        AwsvpcConfiguration:
          Subnets:
            - !If [
                CreateNewVPC,
                !Ref PrivateSubnet1,
                !Ref ExistingPrivateSubnet1,
              ]
            - !If [
                CreateNewVPC,
                !Ref PrivateSubnet2,
                !Ref ExistingPrivateSubnet2,
              ]
          SecurityGroups:
            - !Ref ECSSecurityGroup
          AssignPublicIp: DISABLED
      LoadBalancers:
        - ContainerName: !Sub "${ProjectName}-backend"
          ContainerPort: !Ref ContainerPort
          TargetGroupArn: !Ref ALBTargetGroup
      DeploymentConfiguration:
        MaximumPercent: 200
        MinimumHealthyPercent: 100
        DeploymentCircuitBreaker:
          Enable: true
          Rollback: true
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-${Environment}-service"

  # ============================================================================
  # Auto Scaling
  # ============================================================================
  ECSAutoScalingTarget:
    Type: AWS::ApplicationAutoScaling::ScalableTarget
    Properties:
      MaxCapacity: !Ref MaxCount
      MinCapacity: !Ref MinCount
      ResourceId: !Sub "service/${ECSCluster}/${ECSService.Name}"
      RoleARN: !Sub "arn:aws:iam::${AWS::AccountId}:role/aws-service-role/ecs.application-autoscaling.amazonaws.com/AWSServiceRoleForApplicationAutoScaling_ECSService"
      ScalableDimension: ecs:service:DesiredCount
      ServiceNamespace: ecs

  ECSCPUScalingPolicy:
    Type: AWS::ApplicationAutoScaling::ScalingPolicy
    Properties:
      PolicyName: !Sub "${ProjectName}-${Environment}-cpu-scaling"
      PolicyType: TargetTrackingScaling
      ScalingTargetId: !Ref ECSAutoScalingTarget
      TargetTrackingScalingPolicyConfiguration:
        PredefinedMetricSpecification:
          PredefinedMetricType: ECSServiceAverageCPUUtilization
        TargetValue: 70
        ScaleInCooldown: 300
        ScaleOutCooldown: 60

  ECSMemoryScalingPolicy:
    Type: AWS::ApplicationAutoScaling::ScalingPolicy
    Properties:
      PolicyName: !Sub "${ProjectName}-${Environment}-memory-scaling"
      PolicyType: TargetTrackingScaling
      ScalingTargetId: !Ref ECSAutoScalingTarget
      TargetTrackingScalingPolicyConfiguration:
        PredefinedMetricSpecification:
          PredefinedMetricType: ECSServiceAverageMemoryUtilization
        TargetValue: 80
        ScaleInCooldown: 300
        ScaleOutCooldown: 60

  # Scale on request count per target (helps when LLM/AI endpoints are I/O-bound and CPU stays low)
  ECSRequestCountScalingPolicy:
    Type: AWS::ApplicationAutoScaling::ScalingPolicy
    Properties:
      PolicyName: !Sub "${ProjectName}-${Environment}-request-count-scaling"
      PolicyType: TargetTrackingScaling
      ScalingTargetId: !Ref ECSAutoScalingTarget
      TargetTrackingScalingPolicyConfiguration:
        PredefinedMetricSpecification:
          PredefinedMetricType: ALBRequestCountPerTarget
          ResourceLabel: !Sub
            - "${ALBFullName}/${TGFullName}"
            - ALBFullName: !GetAtt ApplicationLoadBalancer.LoadBalancerFullName
              TGFullName: !GetAtt ALBTargetGroup.TargetGroupFullName
        TargetValue: 500.0
        ScaleInCooldown: 300
        ScaleOutCooldown: 60

  # ============================================================================
  # CloudWatch Alarms
  # ============================================================================
  AlertsTopic:
    Type: AWS::SNS::Topic
    Properties:
      TopicName: !Sub "${ProjectName}-${Environment}-alerts"
      DisplayName: !Sub "${ProjectName} ${Environment} Alerts"

  AlertsEmailSubscription:
    Type: AWS::SNS::Subscription
    Condition: HasAlertEmail
    Properties:
      TopicArn: !Ref AlertsTopic
      Protocol: email
      Endpoint: !Ref AlertEmail

  ECSCPUAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${ProjectName}-${Environment}-ecs-cpu-high"
      AlarmDescription: ECS CPU utilization is high
      MetricName: CPUUtilization
      Namespace: AWS/ECS
      Statistic: Average
      Period: 300
      EvaluationPeriods: 3
      Threshold: 85
      ComparisonOperator: GreaterThanThreshold
      Dimensions:
        - Name: ClusterName
          Value: !Ref ECSCluster
        - Name: ServiceName
          Value: !GetAtt ECSService.Name
      AlarmActions:
        - !Ref AlertsTopic
      OKActions:
        - !Ref AlertsTopic

  ECSMemoryAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${ProjectName}-${Environment}-ecs-memory-high"
      AlarmDescription: ECS memory utilization is high
      MetricName: MemoryUtilization
      Namespace: AWS/ECS
      Statistic: Average
      Period: 300
      EvaluationPeriods: 3
      Threshold: 85
      ComparisonOperator: GreaterThanThreshold
      Dimensions:
        - Name: ClusterName
          Value: !Ref ECSCluster
        - Name: ServiceName
          Value: !GetAtt ECSService.Name
      AlarmActions:
        - !Ref AlertsTopic

Outputs:
  ECRRepositoryUri:
    Description: ECR repository URI
    Value: !Sub "${AWS::AccountId}.dkr.ecr.${AWS::Region}.amazonaws.com/${ECRRepository}"
    Export:
      Name: !Sub "${ProjectName}-${Environment}-ecr-uri"

  ECRRepositoryName:
    Description: ECR repository name
    Value: !Ref ECRRepository

  VPCId:
    Description: VPC ID
    Value: !If [CreateNewVPC, !Ref VPC, !Ref ExistingVPCId]
    Export:
      Name: !Sub "${ProjectName}-${Environment}-vpc-id"

  PrivateSubnet1:
    Description: Private subnet 1 ID
    Value: !If [CreateNewVPC, !Ref PrivateSubnet1, !Ref ExistingPrivateSubnet1]
    Export:
      Name: !Sub "${ProjectName}-${Environment}-private-subnet-1"

  PrivateSubnet2:
    Description: Private subnet 2 ID
    Value: !If [CreateNewVPC, !Ref PrivateSubnet2, !Ref ExistingPrivateSubnet2]
    Export:
      Name: !Sub "${ProjectName}-${Environment}-private-subnet-2"

  ECSClusterName:
    Description: ECS cluster name
    Value: !Ref ECSCluster

  ECSServiceName:
    Description: ECS service name
    Value: !GetAtt ECSService.Name

  ECSSecurityGroupId:
    Description: ECS security group ID (add to Aurora cluster allowed SGs)
    Value: !Ref ECSSecurityGroup
    Export:
      Name: !Sub "${ProjectName}-${Environment}-ecs-sg"

  ALBDNSName:
    Description: Application Load Balancer DNS name
    Value: !GetAtt ApplicationLoadBalancer.DNSName

  ALBFullName:
    Description: ALB full name for CloudWatch metrics
    Value: !GetAtt ApplicationLoadBalancer.LoadBalancerFullName
    Export:
      Name: !Sub "${ProjectName}-${Environment}-alb-full-name"

  TargetGroupFullName:
    Description: Target group full name for CloudWatch metrics
    Value: !GetAtt ALBTargetGroup.TargetGroupFullName
    Export:
      Name: !Sub "${ProjectName}-${Environment}-tg-full-name"

  ALBHostedZoneId:
    Description: ALB hosted zone ID (for Route 53)
    Value: !GetAtt ApplicationLoadBalancer.CanonicalHostedZoneID

  KMSKeyArn:
    Description: KMS key ARN
    Value: !GetAtt EncryptionKey.Arn
    Export:
      Name: !Sub "${ProjectName}-${Environment}-kms-key"

  PodcastAudioBucketName:
    Description: Private bucket storing generated podcast audio assets
    Value: !Ref PodcastAudioBucket

  AlertsTopicArn:
    Description: SNS alerts topic ARN
    Value: !Ref AlertsTopic

  BackendURL:
    Description: Backend API URL
    Value: !If
      - HasCertificate
      - !Sub "https://${ApplicationLoadBalancer.DNSName}"
      - !Sub "http://${ApplicationLoadBalancer.DNSName}"
